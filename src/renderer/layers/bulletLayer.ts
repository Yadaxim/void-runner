import type { BulletInstance } from '../../types';
import type { Camera } from '../camera';

export function renderBulletTrails(_ctx: CanvasRenderingContext2D, _bullets: BulletInstance[], _camera: Camera): void {
  throw new Error('not implemented');
}

export function renderBullets(_ctx: CanvasRenderingContext2D, _bullets: BulletInstance[], _camera: Camera): void {
  throw new Error('not implemented');
}

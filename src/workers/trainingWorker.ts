export type TrainingRequest = { type: "TRAIN"; inputs: number[][]; outputs: number[][]; architecture: { layers: number; neuronsPerLayer: number }; inputSize: number; outputSize: number; epochs: number; }; export type TrainingResponse = { type: "PROGRESS"; epoch: number; loss: number } | { type: "COMPLETE"; weights: number[][] } | { type: "ERROR"; message: string }; self.onmessage = (_event: MessageEvent<TrainingRequest>) => { const response: TrainingResponse = { type: "ERROR", message: "not implemented" }; self.postMessage(response); };
